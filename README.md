# unsplash-crud

MY_website

This is a React + TypeScript + Vite app with Tailwind CSS, multiple pages, Unsplash random backgrounds, and a client-side CRUD with localStorage. Use `npm run dev` to start locally.
